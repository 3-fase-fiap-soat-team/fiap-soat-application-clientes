apiVersion: v1
kind: Service
metadata:
  name: fiap-soat-clientes-service
  namespace: fiap-soat-app
  labels:
    app: fiap-soat-clientes
    service: clientes
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"  # Network Load Balancer
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: fiap-soat-clientes
